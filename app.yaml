command: ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
env:
  - name: DATABRICKS_GATEWAY_URL
    valueFrom: "databricks-gateway-url"
  - name: DATABRICKS_TOKEN
    valueFrom: "databricks-token"
  - name: DATABRICKS_ENDPOINT_NAME
    valueFrom: "databricks-endpoint-name"
